import React from 'react';
import useStyles from './styles';

const Form = () => {
  const classes = useStyles();
  return (
    <div>
      <h2>Form Component</h2>
      <p>This is where you can add your form elements.</p>
    </div>
  );
};

export default Form;
